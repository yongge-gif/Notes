# 启动 Redis 容器                       docker run -d --name redis -p 6379:6379 redis (windows终端执行)
# 查看运行中的容器                       docker ps
# 停止                                 Redis docker stop redis
# 重新启动Redis                         docker start redis
# 删除Redis容器                         docker rm -f redis